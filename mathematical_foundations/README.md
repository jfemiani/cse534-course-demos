# Mathematical Foundations demonstrations

Reviewed Python demonstrations used in the CSE 534 Mathematical Foundations module. Each script prints sample rows before comparing observed results with the probability distribution that generated them.

Install the dependencies with:

    python -m pip install -r requirements-math-foundations.txt
